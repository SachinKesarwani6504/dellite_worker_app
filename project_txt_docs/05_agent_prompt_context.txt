AGENT PROMPT CONTEXT (COPY-PASTE TEMPLATE)

Use this prompt when asking any coding agent to build customer app:

"""
Project: Build Dellite Customer App from current Dellite Worker App baseline.

Hard requirements:
1) Keep same architecture, folder structure style, and coding conventions.
2) Keep same UI language: spacing, card styles, color tokens, dark mode behavior, and common components.
3) Keep same library stack unless explicitly not needed.
4) Customer onboarding should be one screen only with firstName + lastName.
5) Remove worker-only onboarding logic (services/certifications).
6) Use /auth/me with CUSTOMER role to decide onboarding completion.
7) Keep code scalable and reusable (shared components/hooks/utilities).
8) Preserve import alias pattern: @/...

Deliverables expected from agent:
- Full folder/file scaffolding for customer app.
- Navigation setup (Auth, Onboarding, Tabs).
- AuthContext with CUSTOMER onboarding flag handling.
- Customer onboarding screen implementation.
- List of installed dependencies and setup commands.
- Type-check clean code.
"""

NOTES
- If backend response fields are inconsistent, normalize booleans defensively.
- Back button should render only when canGoBack() is true.
- RefreshControl should be direct native element in ScrollView props.
